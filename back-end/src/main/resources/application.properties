# MySQL Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/cms?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=True
spring.datasource.username=pguelfi
spring.datasource.password=pg123456
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA/Hibernate Configuration for MySQL (This will create your tables automatically)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# MongoDB Configuration
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=prescriptions
# Name of the MongoDB database, matching lab instructions

# Server Port
server.port=8080

# Static resource locations
spring.web.resources.static-locations=classpath:/static/

# Thymeleaf template configuration
spring.thymeleaf.prefix=classpath:/templates/
spring.thymeleaf.suffix=.html
spring.thymeleaf.mode=HTML
spring.thymeleaf.cache=false
spring.thymeleaf.encoding=UTF-8

# application.properties
jwt.secret=your_super_secret_key_here_at_least_256bit_or_32_chars_long

# application.properties
api.path=/api/v1
